#include "main.h"
#include <stdio.h>

/**
 * main -checks the code
 * Description: find the sum of two integers
 * Return: Always 0
 */
int main(void)
{
	int n;

	n = add(89, 9);
	printf("%d\n", n);
	return (0);
}
