#include <stdio.h>
/**
 * main - main function
 * @argc: number of command line arguments
 * @argv: an array of strings containing cmd arguments
 * Return: 0
 */
int main (int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
